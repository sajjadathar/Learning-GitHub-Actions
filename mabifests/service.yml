apiVersion: v1
kind: Service

metadata:
  name: sajjadathar-service
  labels:
    app: sajjadathar-app
spec:
  type: NodePort
  selector:
    app: sajjadathar-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30087